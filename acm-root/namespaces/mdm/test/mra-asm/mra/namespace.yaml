# creates namespace named "mra"
apiVersion: v1
kind: Namespace
metadata:
  name: mra
  annotations:
    configsync.gke.io/cluster-name-selector: askdemocluster3
    #configmanagement.gke.io/cluster-selector: mdm-test-clusters
  labels:
    istio-injection: enabled
    owned-by-businessunit: mdm
